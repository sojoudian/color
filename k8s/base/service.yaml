apiVersion: v1
kind: Service
metadata:
  name: color
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: color
  ports:
    - name: http
      port: 80
      targetPort: http
      appProtocol: http
